.DS_Store
*.log
node_modules/
.turbo
.idea/
.playwright-mcp/

# Go test coverage profile.
coverage.out

# Private migration artifacts should stay outside git.
*.tar.gz
*.tar.gz.age
macos-settings-archives/
archives/
# Legacy turbo / pnpm cache holder (cacheDir now points to .turbo/cache).
# Keep ignored so an existing local checkout doesn't dirty the working tree.
storage/.cache
storage/.pnpm-store
# Secrets and generated local state.
*.pem
*.key
id_*
!*.pub
.zsh_history
.bash_history
.mysql_history
auth.json
hosts.yml
ngrok.yml
state.vscdb
storage.json
