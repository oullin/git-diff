{
  "name": "git-diff-review",
  "private": true,
  "scripts": {
    "build": "pnpm --dir packages/tools run turbo -- run build",
    "test": "pnpm --dir packages/tools run turbo -- run test",
    "dev": "pnpm --dir packages/tools run turbo -- run dev --filter=ui",
    "format": "pnpm --dir packages/tools run turbo -- run format",
    "format:check": "pnpm exec oxfmt --check packages/ui packages/bridge/src packages/bridge/package.json packages/bridge/tsconfig.json package.json turbo.json",
    "lint": "pnpm exec oxlint --vue-plugin packages/ui packages/bridge",
    "tooling:check": "pnpm --dir packages/ui exec tsx scripts/check-tooling-policy.ts ../..",
    "check": "pnpm run tooling:check && pnpm --dir packages/tools run turbo -- run build test",
    "release:mac:unsigned": "pnpm --dir packages/tools run release:mac:unsigned"
  },
  "devDependencies": {
    "@types/node": "^25.6.2",
    "oxfmt": "0.48.0",
    "oxlint": "1.63.0",
    "tsx": "^4.21.0",
    "turbo": "2.9.12"
  },
  "packageManager": "pnpm@10.33.0",
  "pnpm": {
    "overrides": {
      "@ungap/structured-clone": "^1.3.1"
    }
  }
}
